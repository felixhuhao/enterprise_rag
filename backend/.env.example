# ---- API / app ----
# Frontend requests use Authorization: Bearer <token>.
# Use a private value for shared deployments.
API_TOKEN=
RATE_LIMIT_PER_MINUTE=60
CORS_ORIGINS=["http://localhost:5173","http://localhost:4173"]

# ---- Local storage ----
GENERAL_UPLOAD_DIR=./data/general_uploads
GENERAL_PARSED_DIR=./data/general_parsed
DATABASE_PATH=./data/app.db
STORAGE_MIN_FREE_MB=1024

# ---- LLM service (DeepSeek-compatible) ----
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
CHAT_MODEL=deepseek-v4-flash
CHAT_TIMEOUT=180
CHAT_TEMPERATURE=0.0
CHAT_MAX_TOKENS=1600
HYDE_TEMPERATURE=0.3
HYDE_MAX_TOKENS=256
QUERY_EXPANSION_TEMPERATURE=0.3
QUERY_EXPANSION_MAX_TOKENS=256
RERANK_MAX_TOKENS=128
GROUNDEDNESS_TEMPERATURE=0.0
GROUNDEDNESS_MAX_TOKENS=1800
INTENT_CLASSIFIER_MODEL=
INTENT_CLASSIFIER_TEMPERATURE=0.0
INTENT_CLASSIFIER_MAX_TOKENS=256
INTENT_CLASSIFIER_TIMEOUT=30

# ---- Local model endpoint (optional) ----
# Leave empty unless a local/vLLM-compatible model endpoint is used.
LOCAL_MODEL_URL=
LOCAL_MODEL_NAME=

# ---- Embedding ----
# EMBEDDING_PROVIDER: local (BGE-M3/FlagEmbedding) or qwen (remote Qwen text-embedding-v4).
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL_NAME=bge-m3
EMBEDDING_MODEL_PATH=/home/hao/models/BAAI/bge-m3
EMBEDDING_DIM=1024
EMBEDDING_BATCH_SIZE=4
EMBEDDING_MAX_LENGTH=8192
EMBEDDING_DEVICE=auto
EMBEDDING_USE_FP16=true
# Remote embedding reliability (provider=qwen only).
EMBEDDING_TIMEOUT=30
EMBEDDING_MAX_RETRIES=2

# ---- Qwen / DashScope (used when EMBEDDING_PROVIDER=qwen or IMAGE_DESCRIPTION_PROVIDER=qwen) ----
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ---- MinerU PDF parsing (optional; PDF parsing is unavailable without token) ----
MINERU_BASE_URL=https://mineru.net/api/v4
MINERU_API_TOKEN=
MINERU_MODEL_VERSION=vlm
MINERU_POLL_INTERVAL=3
MINERU_POLL_TIMEOUT=1800
MINERU_UPLOAD_TIMEOUT=300
MINERU_DOWNLOAD_TIMEOUT=300

# ---- Milvus ----
MILVUS_URI=http://localhost:19530
MILVUS_REQUIRED_ON_STARTUP=false
MILVUS_HEALTH_TIMEOUT_SECONDS=2.0

# ---- Upload limits ----
MD_ZIP_MAX_SIZE_MB=50
UPLOAD_MAX_SIZE_MB=100

# ---- Chunk search enrichment ----
CHUNK_ENRICHMENT_ENABLED=false
CHUNK_ENRICHMENT_PROFILE=none

# ---- Image description ----
# IMAGE_DESCRIPTION_PROVIDER: qwen (Qwen-VL) or zhipu (GLM-4.6V).
IMAGE_DESCRIPTION_PROVIDER=qwen
ZHIPU_API_KEY=your-zhipu-api-key
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4
IMAGE_DESCRIPTION_ENABLED=true
IMAGE_DESCRIPTION_MODEL=qwen3-vl-flash
IMAGE_DESCRIPTION_CONCURRENCY=3
IMAGE_DESCRIPTION_TIMEOUT=30
IMAGE_DESCRIPTION_MAX_TOKENS=800
IMAGE_DESCRIPTION_MAX_SIZE_MB=10
