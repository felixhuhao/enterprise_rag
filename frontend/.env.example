# Backend API target (used by Vite dev server proxy)
# Default: http://localhost:8010
# For Docker: http://backend:8010
VITE_API_TARGET=http://localhost:8010

# Optional experimental tag governance UI.
# Hidden by default because rule-based chunk enrichment is disabled pending redesign.
VITE_ENABLE_TAG_GOVERNANCE=false
