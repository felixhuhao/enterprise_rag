## 重复段

同一段制度说明会被重复切片，同一段制度说明会被重复切片。该段用于验证质量分析器是否能识别重复内容，并且不会因为章节标题不同就忽略内容重复。

## 重复段

同一段制度说明会被重复切片，同一段制度说明会被重复切片。该段用于验证质量分析器是否能识别重复内容，并且不会因为章节标题不同就忽略内容重复。

## 正常段

星辰科技 API 网关配置说明，包含调用上限、认证策略、告警规则、审计要求、异常处理和升级路径。该段内容与重复段不同，用于确认非重复内容不会被误报。
