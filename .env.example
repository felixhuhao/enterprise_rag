# ---- API 鉴权 ----
# 前端请求时需携带 Authorization: Bearer <token>
API_TOKEN=enterprise-rag-dev-token

# ---- LLM 服务 (DeepSeek) — 必填 ----
DEEPSEEK_API_KEY=sk-your-deepseek-api-key
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
CHAT_MODEL=deepseek-v4-flash

# ---- VL 图像描述 (智谱 GLM-4.6V) — 可选 ----
ZHIPU_API_KEY=your-zhipu-api-key
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LOCAL_MODEL_URL=
LOCAL_MODEL_NAME=

# ---- Embedding (本地 dense embedding 模型；默认使用 BAAI/bge-m3) ----
# Docker 使用 EMBEDDING_MODEL_HOST_PATH 挂载宿主机模型目录到容器 /models/embedding
EMBEDDING_MODEL_NAME=bge-m3
EMBEDDING_MODEL_HOST_PATH=/mnt/d/Models/BAAI/bge-m3
EMBEDDING_MODEL_PATH=/models/embedding
EMBEDDING_DIM=1024
EMBEDDING_BATCH_SIZE=4
EMBEDDING_MAX_LENGTH=8192
EMBEDDING_DEVICE=auto
EMBEDDING_USE_FP16=true

# ---- MinerU PDF 解析 — 可选，未设置时 PDF 解析不可用 ----
MINERU_BASE_URL=https://mineru.net/api/v4
MINERU_API_TOKEN=
MINERU_MODEL_VERSION=vlm

# ---- Milvus ----
# Docker Compose 启动时由 docker-compose.yml 自动覆盖为 http://milvus-standalone:19530
# 本地开发: http://localhost:19530
MILVUS_URI=http://localhost:19530
MILVUS_REQUIRED_ON_STARTUP=false
MILVUS_HEALTH_TIMEOUT_SECONDS=2.0

# ---- Storage / health checks ----
STORAGE_MIN_FREE_MB=1024
